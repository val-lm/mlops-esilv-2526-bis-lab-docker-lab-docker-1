docker network create lab-net
docker run -d --name back-container --network lab-net -p 5000:5000 -e CURRENT_ENVIRONMENT=dev valentiiiine/lab_docker:latest
docker run -d --name front-container --network lab-net -p 8080:80 -e TR_WS_BACK_URL=http://back-container:5000 flask-front